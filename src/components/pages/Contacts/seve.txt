
                {!addingMode && (<Row className="ListBoxContainer">
                    <label className="col-md-3 form-label mb-4">
                        Categories:
                    </label>

                    <div className="mb-4 AddingForm" >

                        <input className="Dan-input-text" type="text" id="Categorie" />

                        <Button variant="primary" onClick={handleAdd}>
                            Modifier
                        </Button>
                    </div>

                    <div className="mb-4 ListBox">
                        <div className="ListBoxContent">
                            <select className="Dan-select-multiple" id="ListeCategoriesEdit" size="10"  multiple>
                                {renderOptions()}
                            </select>
                        </div>
                        <div className="ListBoxBtns">
                                <div style={{marginBottom: '20px'}}>
                                    <Button variant="primary" onClick={handleUpEdit}>
                                        <GoArrowSmallUp />
                                    </Button>
                                </div>
                                <div>
                                    <Button variant="primary" onClick={handleDownEdit}>
                                       <GoArrowSmallDown />
                                    </Button>
                                </div>
                        </div>
                    </div>

                </Row>
              )}